streamlit>=1.32
beautifulsoup4
lxml
requests
curl_cffi>=0.7  # TLS 지문 위장 — WAF 차단 사이트(thebell 등) 폴백(scraping/http.fetch_impersonated)
pandas
pyarrow
openpyxl
openai>=1.40
python-dotenv
fastapi>=0.110          # React 전환용 백엔드 API (api/)
uvicorn>=0.27           # ASGI 서버 — `uvicorn api.main:app`
httpx>=0.27             # FastAPI TestClient + 향후 API 클라이언트
pytest
